#include <iostream>
#include <cstdarg>
#include "debug.hpp"

/*
void LOG(const char *fmt, ...) {
  va_list args;
  va_start(args, fmt);

  printf(fmt, args);

  va_end(args);
}

void ABORT(const char *fmt, ...) {
  va_list args;
  va_start(args, fmt);

  printf(fmt, args);

  va_end(args);

  exit(-1);
}
*/